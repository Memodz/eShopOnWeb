name: Test Runner
on: workflow_dispatch
jobs:
  test-runner:
    runs-on: 
      - self-hosted
      - docker
      - javascritp
    steps:
      - run: echo "Test sefthost runner"
      - name: Checkout
        uses: action/checkout@v7.0.0
